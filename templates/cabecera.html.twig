{# templates/cabecera.html.twig #}
{% block header %}
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top border-bottom">
        <div class="container">
            <a class="navbar-brand fw-bold fs-4 text-primary" href="{{ path('categorias') }}">
                <span class="me-1">🛍️</span> Perfume <span class="text-dark">Shop</span>
            </a>

            <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarContent" aria-controls="navbarContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
                    <li class="nav-item">
                        <a class="nav-link fw-medium {% if app.request.attributes.get('_route') == 'home' %}active text-primary{% endif %}"
                           href="{{ path('home') }}">Inicio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link fw-medium {% if app.request.attributes.get('_route') == 'categorias' %}active text-primary{% endif %}"
                           href="{{ path('categorias') }}">Categorías</a>
                    </li>
                </ul>

                <div class="d-flex align-items-center">
                    {% if app.user %}
                        <span class="navbar-text me-3 d-none d-lg-inline">
                            Hola, <span class="fw-bold text-dark">{{ app.user.login }}</span>
                        </span>

                        <div class="btn-group">
                            {% if is_granted('ROLE_ADMIN') %}
                                <a class="btn btn-outline-danger btn-sm px-3" href="{{ path('admin') }}">
                                    ⚙️ Admin
                                </a>
                            {% endif %}

                            <a class="btn btn-light btn-sm px-3 border" href="{{ path('historial') }}">
                                📦 Pedidos
                            </a>

                            <a class="btn btn-primary btn-sm px-3 position-relative" href="{{ path('cesta') }}">
                                🛒 Cesta
                                {# Opcional: podrías añadir aquí un badge con el número de items #}
                            </a>

                            <a class="btn btn-dark btn-sm px-3" href="{{ path('logout') }}" title="Cerrar sesión">
                                🚪
                            </a>
                        </div>
                    {% else %}
                        <a class="btn btn-primary rounded-pill px-4" href="{{ path('login') }}">
                            Iniciar sesión
                        </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </nav>
{% endblock %}