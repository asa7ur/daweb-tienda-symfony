{% extends 'base.html.twig' %}

{% block title %}Productos{% endblock %}

{% block contenido %}
<div class="container">
    <h1 class="mb-4 text-center">Productos disponibles</h1>

    {% if productos is empty %}
        <div class="alert alert-warning text-center">
            No hay productos en esta categoría.
        </div>
    {% else %}
        <div class="row g-4">
            {% for producto in productos %}
                <div class="col-12 col-md-6 col-lg-4">
                    <div class="card h-100 shadow-sm border-0">
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title">{{ producto.getNombreCorto() }}</h5>
                            <h6 class="text-muted">{{ producto.getNombre() }}</h6>
                            <p class="card-text mt-2">
                                {{ producto.getDescripcion()|slice(0, 150) ~ '...' }}
                            </p>
                            <div class="mt-auto">
                                <p class="fw-bold mb-2">💶 {{ producto.getPrecio() }} €</p>
                                <a href="{{ path('anadir', {'id': producto.getId()}) }}" class="btn btn-primary w-100">
                                    🛒 Añadir al carrito
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}
</div>
{% endblock %}
